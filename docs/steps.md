Downloaded example

```
npx try-prisma@latest --template typescript/graphql-nestjs
```

Run postgressql
docker compose up -d

```
npm install
make generate
npm run migrate
```

Add data to db
make seed

launch

```
npm run dev
```
